---
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/apps/orders/__init__.py
  - backend/apps/orders/apps.py
  - backend/apps/orders/models.py
  - backend/apps/orders/admin.py
  - backend/apps/shipping/__init__.py
  - backend/apps/shipping/apps.py
  - backend/apps/shipping/models.py
  - backend/apps/shipping/admin.py
  - backend/apps/production/__init__.py
  - backend/apps/production/apps.py
  - backend/apps/production/models.py
  - backend/apps/production/admin.py
  - backend/mike_shirts/settings/base.py
autonomous: true
objective: "Create Order, OrderItem, SaleTicket, ShipTicket, and ProductionTicket models with full Django Admin so the team can see and manage orders and tickets immediately."
must_haves:
  - "Order model with status flow: pending_payment → paid → in_production → shipped → delivered | cancelled"
  - "OrderItem with variant FK and price/name snapshots"
  - "SaleTicket 1:1 with Order, auto-folio"
  - "ShipTicket 1:1 with Order, manual fields (tracking, carrier, cost, status)"
  - "ProductionTicket per OrderItem, status flow: pending → in_production → completed"
  - "Shipping address stored as JSON snapshot on Order"
  - "Django Admin configured for all models"
---

# Plan 02: Orders + Shipping + Production Models + Admin

## Objective

Create the data layer for orders and all three ticket types. Configure Django Admin so the ops team can manage everything.

## Tasks

<task id="03-02-01">
<name>Create orders app with Order, OrderItem, SaleTicket models</name>
<action>
1. Order: user FK, shipping_address (JSONField snapshot), status (choices), subtotal, shipping_cost, total, mp_preference_id (blank), mp_payment_id (blank), created_at, updated_at.
2. OrderItem: order FK, variant FK (SET_NULL nullable), quantity, unit_price, product_name, variant_desc (snapshots).
3. SaleTicket: order (OneToOne), folio (auto CharField), issued_at. Folio format: MS-YYYYMMDD-NNNN.
</action>
<done>[ ]</done>
</task>

<task id="03-02-02">
<name>Create shipping and production apps with ticket models</name>
<action>
1. ShipTicket: order (OneToOne), tracking_number (blank), carrier (blank), shipping_cost, status (pending/shipped/delivered), shipped_at (null), delivered_at (null).
2. ProductionTicket: order_item FK, variant FK (SET_NULL nullable), quantity_needed, status (pending/in_production/completed), estimated_completion (DateField), completed_at (null).
</action>
<done>[ ]</done>
</task>

<task id="03-02-03">
<name>Register all apps, create migrations, configure admin</name>
<action>
1. Add apps to INSTALLED_APPS, makemigrations, migrate.
2. OrderAdmin with OrderItemInline, SaleTicket inline (readonly).
3. ShipTicketAdmin with filters by status.
4. ProductionTicketAdmin with filters by status.
</action>
<done>[ ]</done>
</task>

## Must-Haves

- [x] Order status flow works
- [x] SaleTicket auto-generates folio
- [x] ShipTicket has all manual fields for shipping team
- [x] ProductionTicket tracks per-item production needs
- [x] All Django Admin configured with inlines and filters
