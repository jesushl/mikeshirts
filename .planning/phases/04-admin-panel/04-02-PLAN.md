---
wave: 2
depends_on: ["04-01"]
files_modified:
  - backend/apps/admin_panel/serializers.py
  - backend/apps/admin_panel/views.py
  - backend/apps/admin_panel/urls.py
autonomous: true
objective: "Build role-specific API endpoints for ventas (orders/tickets), producción (production tickets CRUD), and envíos (shipping tickets CRUD) so each team can operate from the React frontend."
must_haves:
  - "GET /api/admin/ventas/orders/ lists all orders with filters (status, date range)"
  - "GET /api/admin/ventas/tickets/ lists sale tickets with search by folio"
  - "GET /api/admin/ventas/metrics/ returns sales period metrics"
  - "GET /api/admin/production/tickets/ lists production tickets with status filter"
  - "PATCH /api/admin/production/tickets/<id>/ updates status"
  - "GET /api/admin/shipping/tickets/ lists shipping tickets with status filter"
  - "PATCH /api/admin/shipping/tickets/<id>/ updates tracking, carrier, cost, status"
---

# Plan 02: Ventas, Producción, Envíos Endpoints

## Tasks

<task id="04-02-01">
<name>Ventas endpoints — orders, sale tickets, metrics</name>
<action>
1. Serializers for admin order list (with user email, status, total).
2. AdminOrderListView with filters (status, date range). Permission: IsVentas.
3. AdminSaleTicketListView with search by folio.
4. VentasMetricsView: total_sales, order_count, avg_ticket for a date range.
</action>
<done>[ ]</done>
</task>

<task id="04-02-02">
<name>Production endpoints — list and update tickets</name>
<action>
1. ProductionTicketListView: filterable by status. Permission: IsProduccion.
2. ProductionTicketUpdateView: PATCH status, completed_at.
</action>
<done>[ ]</done>
</task>

<task id="04-02-03">
<name>Shipping endpoints — list and update tickets</name>
<action>
1. ShippingTicketListView: filterable by status. Permission: IsEnvios.
2. ShippingTicketUpdateView: PATCH tracking_number, carrier, shipping_cost, status, shipped_at, delivered_at.
</action>
<done>[ ]</done>
</task>

## Must-Haves

- [x] Ventas sees orders and sale tickets, not shipping/production
- [x] Producción can update ticket status
- [x] Envíos can capture tracking info
- [x] admin_full sees everything
