---
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/apps/catalog/views.py
  - backend/apps/catalog/sitemaps.py
  - backend/mike_shirts/urls.py
autonomous: true
objective: "Add SEO backend endpoints — sitemap.xml with products/categories, robots.txt, JSON-LD Product schema endpoint, llms.txt, and image format specs for Meta (1:1, 4:5, 9:16)."
must_haves:
  - "GET /sitemap.xml returns valid XML sitemap with products and categories"
  - "GET /robots.txt returns proper robots file"
  - "GET /api/catalog/products/<slug>/schema/ returns JSON-LD Product markup"
  - "GET /llms.txt returns structured store description"
  - "ProductDetailSerializer includes image format URLs (meta_formats field)"
---

# Plan 02: SEO Backend — Sitemap, Robots, JSON-LD, llms.txt, Meta Formats

## Must-Haves

- [x] sitemap.xml with product and category URLs
- [x] robots.txt allows crawlers, points to sitemap
- [x] JSON-LD Product schema endpoint per product
- [x] llms.txt describes the store for LLMs
- [ ] Meta image formats — deferred to frontend (needs actual images uploaded first)
