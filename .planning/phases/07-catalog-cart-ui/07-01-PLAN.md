---
wave: 1
depends_on: ["06-01"]
files_modified:
  - backend/apps/catalog/models.py
  - backend/apps/catalog/filters.py
  - backend/apps/catalog/serializers.py
  - backend/apps/catalog/migrations/XXXX_product_is_featured.py
  - frontend/src/services/catalog.ts
  - frontend/src/hooks/useCatalog.ts
  - frontend/src/pages/CatalogPage.tsx
  - frontend/src/components/ProductCard.tsx
  - frontend/src/components/CatalogFilters.tsx
  - frontend/src/components/FilterSheet.tsx
  - frontend/src/components/AvailabilityBadge.tsx
  - frontend/src/App.tsx
autonomous: true
objective: "Backend is_featured field + CatalogPage con grid de productos, filtros (sidebar desktop / sheet mobile), búsqueda y paginación."
must_haves:
  - "is_featured field on Product model with migration"
  - "featured filter on ProductFilter + exposed in list serializer"
  - "CatalogPage with responsive product grid"
  - "ProductCard with image, name, price, availability badge"
  - "Filters: category, size, color, price range, featured"
  - "Search bar with text search"
  - "Pagination (load more or numbered)"
  - "Mobile filter sheet (slide-up modal)"
  - "Desktop sidebar filters"
---

# Plan 07-01 — Backend is_featured + Catálogo UI

## Objective

Add `is_featured` to Product model (backend) and build the full catalog browsing page with filters, search, and pagination.

## Context

- Backend API already supports `GET /api/catalog/products/` with filtering (category, size, color, min_price, max_price), search, ordering, and pagination.
- Types `ProductListItem`, `Category`, `PaginatedResponse` already defined in `frontend/src/types/index.ts`.
- API client with JWT interceptor ready in `frontend/src/api/client.ts`.
- `formatMXN` and `availabilityLabel` helpers exist in `frontend/src/utils/format.ts`.
- Placeholder route `/catalogo` exists in App.tsx.
- Color choices: negro, blanco, gris, azul_marino, rojo, verde.
- Size choices: XS, S, M, L, XL, XXL.
- Paleta: #0a0a0a fondo, #e11d48 accent, #06b6d4 cyan, #f5f5f5 texto, #141414 surface.

## Tasks

<task id="07-01-01">
<name>Add is_featured field to Product model</name>
<files>
- backend/apps/catalog/models.py
- backend/apps/catalog/filters.py
- backend/apps/catalog/serializers.py
</files>
<action>
1. Add `is_featured = models.BooleanField('Destacado', default=False)` to Product model.
2. Add `featured = django_filters.BooleanFilter(field_name='is_featured')` to ProductFilter.
3. Add `is_featured` to ProductListSerializer fields.
4. Run `python manage.py makemigrations catalog` and `python manage.py migrate`.
</action>
<verify>
- `python manage.py check` passes
- `python manage.py migrate` applies cleanly
- `GET /api/catalog/products/?featured=true` returns only featured products
</verify>
<done>[ ]</done>
</task>

<task id="07-01-02">
<name>Create catalog API service</name>
<files>
- frontend/src/services/catalog.ts
</files>
<action>
1. Create `services/catalog.ts` with functions:
   - `fetchCategories()` → `Category[]` — GET /api/catalog/categories/
   - `fetchProducts(params)` → `PaginatedResponse<ProductListItem>` — GET /api/catalog/products/ with query params for category, size, color, min_price, max_price, search, ordering, page
   - `fetchProduct(slug)` → `ProductDetail` — GET /api/catalog/products/{slug}/ (used in Wave 2 but define now)
2. All functions use the existing `api` client from `../api/client`.
3. Params type: `CatalogFilters` with optional fields matching the backend filter params.
</action>
<verify>
- TypeScript compiles
- Import works from other modules
</verify>
<done>[ ]</done>
</task>

<task id="07-01-03">
<name>Create AvailabilityBadge component</name>
<files>
- frontend/src/components/AvailabilityBadge.tsx
</files>
<action>
1. Create a small component that takes `availability: string` and optional `productionDays: number`.
2. Renders a styled badge:
   - `in_stock` → green dot + "Disponible"
   - `made_to_order` → cyan dot + "Se produce en X días"
   - `unavailable` → gray dot + "Agotado"
3. Uses `availabilityLabel` from utils/format.ts.
</action>
<verify>
- TypeScript compiles
</verify>
<done>[ ]</done>
</task>

<task id="07-01-04">
<name>Create ProductCard component</name>
<files>
- frontend/src/components/ProductCard.tsx
</files>
<action>
1. Create ProductCard that takes a `ProductListItem` prop.
2. Renders: image (with fallback placeholder if null), product name, formatted price (formatMXN), AvailabilityBadge, category name.
3. Entire card is a Link to `/producto/{slug}`.
4. Image has aspect-ratio: 1/1 with object-cover.
5. Hover effect: subtle scale or border highlight using accent color.
6. Responsive: works in 2-col (mobile), 3-col (tablet), 4-col (desktop) grid.
</action>
<verify>
- TypeScript compiles
</verify>
<done>[ ]</done>
</task>

<task id="07-01-05">
<name>Create CatalogFilters sidebar component</name>
<files>
- frontend/src/components/CatalogFilters.tsx
</files>
<action>
1. Receives current filters and an `onFilterChange` callback.
2. Sections:
   - **Categorías**: fetch from API, render as clickable list with active highlight.
   - **Talla**: checkboxes/buttons for XS, S, M, L, XL, XXL.
   - **Color**: color swatches (circles with bg color + label). Map to model values: negro→#0a0a0a, blanco→#f5f5f5, gris→#6b7280, azul_marino→#1e3a5f, rojo→#e11d48, verde→#16a34a.
   - **Precio**: min/max inputs or a simple range selector.
   - **Ordenar**: dropdown — Recientes, Precio ↑, Precio ↓, Nombre A-Z.
3. "Limpiar filtros" button to reset all.
4. Only visible on desktop (hidden md:block). Mobile uses FilterSheet.
</action>
<verify>
- TypeScript compiles
</verify>
<done>[ ]</done>
</task>

<task id="07-01-06">
<name>Create FilterSheet mobile component</name>
<files>
- frontend/src/components/FilterSheet.tsx
</files>
<action>
1. Full-screen or bottom-sheet modal that slides up on mobile.
2. Contains the same filter UI as CatalogFilters (extract shared filter sections or reuse).
3. Triggered by a "Filtros" button visible only on mobile (md:hidden).
4. Has "Aplicar" (close + apply) and "Limpiar" buttons.
5. Backdrop overlay with close on outside click.
6. Body scroll lock when open.
</action>
<verify>
- TypeScript compiles
</verify>
<done>[ ]</done>
</task>

<task id="07-01-07">
<name>Create useCatalog hook</name>
<files>
- frontend/src/hooks/useCatalog.ts
</files>
<action>
1. State: products[], categories[], isLoading, error, filters (CatalogFilters), pagination (count, next, page).
2. On mount: fetch categories, fetch first page of products.
3. When filters change: reset page to 1, re-fetch products.
4. `loadMore()` function for pagination (append to products array).
5. `setFilter(key, value)` to update individual filter and trigger fetch.
6. `resetFilters()` to clear all and re-fetch.
7. Uses URL search params to persist filter state (useSearchParams) so URLs are shareable.
</action>
<verify>
- TypeScript compiles
</verify>
<done>[ ]</done>
</task>

<task id="07-01-08">
<name>Create CatalogPage and wire routes</name>
<files>
- frontend/src/pages/CatalogPage.tsx
- frontend/src/App.tsx
</files>
<action>
1. CatalogPage layout:
   - Top: search bar (full width) with debounced input.
   - Below: 2-column layout on desktop → left sidebar (CatalogFilters, ~280px), right content area.
   - Mobile: search + "Filtros" button + product grid.
   - Product grid: responsive columns (2 mobile, 3 tablet, 4 desktop) using ProductCard.
   - Bottom: "Cargar más" button if more pages available, or numbered pagination.
   - Empty state: "No encontramos productos con esos filtros" with reset button.
   - Loading state: skeleton grid (gray pulsing placeholders).
2. Active filter pills shown above the grid (click to remove individual filter).
3. Results count: "X productos encontrados".
4. Replace placeholder `/catalogo` route in App.tsx with CatalogPage import.
</action>
<verify>
- TypeScript compiles with zero errors
- Vite dev server starts
- `/catalogo` renders the catalog page
- Filters update URL params and re-fetch
- Search works with debounce
- Pagination loads more products
- Mobile filter sheet opens and applies
</verify>
<done>[ ]</done>
</task>

## Must-Haves

- [x] is_featured field on Product with migration applied
- [x] featured filter works on API
- [x] CatalogPage with responsive product grid
- [x] ProductCard with image, name, price, availability badge
- [x] Desktop sidebar filters (category, size, color, price, sort)
- [x] Mobile filter sheet
- [x] Text search with debounce
- [x] Pagination (load more)
- [x] URL params for shareable filter state
- [x] Empty and loading states
