---
wave: 4
depends_on: ["08-03"]
files_modified:
  - frontend/src/pages/legal/ShippingPolicyPage.tsx
  - frontend/src/pages/legal/ReturnsPage.tsx
  - frontend/src/pages/legal/PrivacyPage.tsx
  - frontend/src/components/SeoHead.tsx
  - frontend/src/pages/ProductPage.tsx
  - frontend/src/pages/CatalogPage.tsx
  - frontend/src/pages/HomePage.tsx
  - frontend/src/App.tsx
autonomous: true
objective: "Páginas legales con contenido real (PROFECO), meta tags dinámicos con react-helmet-async, Open Graph para productos, JSON-LD inyectado, verificación responsive final."
must_haves:
  - "ShippingPolicyPage with real shipping policy content"
  - "ReturnsPage with real returns/refund policy (PROFECO)"
  - "PrivacyPage with real privacy notice (LFPDPPP)"
  - "SeoHead component for reusable Helmet patterns"
  - "Dynamic meta tags on all major pages (title, description, OG)"
  - "ProductPage Open Graph tags (og:title, og:description, og:image, og:url)"
  - "ProductPage JSON-LD injection"
  - "All placeholder routes replaced"
---

# Plan 08-04 — Legal + SEO + Polish

## Objective

Create legal pages with real Mexican regulation content, add dynamic SEO meta tags to all pages, inject Open Graph and JSON-LD for products, and do a final responsive verification pass.

## Context

- react-helmet-async already installed and HelmetProvider active (from 08-01).
- Backend serves JSON-LD at `/api/catalog/products/<slug>/schema/`.
- Backend already has: robots.txt, sitemap.xml, llms.txt.
- SITE_URL in backend settings for canonical URLs.
- Legal pages are for Mexican market: PROFECO consumer rights, LFPDPPP privacy law.
- Placeholder routes for /envios, /devoluciones, /privacidad exist in App.tsx.
- Shipping cost is flat $249 MXN, Mexico only, no fixed carrier.

## Tasks

<task id="08-04-01">
<name>Create SeoHead component</name>
<files>
- frontend/src/components/SeoHead.tsx
</files>
<action>
1. Wrapper around react-helmet-async `<Helmet>`.
2. Props: title, description, ogImage?, ogUrl?, ogType?, noindex?
3. Renders:
   - `<title>{title} — Mike Shirts</title>` (appends brand)
   - `<meta name="description">`
   - `<meta property="og:title">`
   - `<meta property="og:description">`
   - `<meta property="og:image">` (default to logo if not provided)
   - `<meta property="og:url">`
   - `<meta property="og:type">` (default "website")
   - `<meta property="og:site_name" content="Mike Shirts">`
   - `<meta name="twitter:card" content="summary_large_image">`
   - Optional `<meta name="robots" content="noindex">` for admin/checkout pages.
4. Used across all pages for consistent SEO.
</action>
<verify>
- TypeScript compiles
</verify>
<done>[ ]</done>
</task>

<task id="08-04-02">
<name>Create ShippingPolicyPage</name>
<files>
- frontend/src/pages/legal/ShippingPolicyPage.tsx
</files>
<action>
1. Real content covering:
   - Cobertura: envíos a todo México.
   - Costo: tarifa única de $249 MXN por pedido.
   - Tiempos de entrega: 5-10 días hábiles (stock disponible), 10-15 días (producción on-demand).
   - Paquetería: variable, se asigna la mejor opción según destino.
   - Seguimiento: número de guía proporcionado por email cuando el paquete se envía.
   - Problemas de entrega: contactar a contacto@mikeshirts.mx.
2. Styled as prose with headings, dark theme, max-width readable.
3. SeoHead with appropriate title and description.
</action>
<verify>
- TypeScript compiles
- Content reads correctly
</verify>
<done>[ ]</done>
</task>

<task id="08-04-03">
<name>Create ReturnsPage</name>
<files>
- frontend/src/pages/legal/ReturnsPage.tsx
</files>
<action>
1. Real content based on PROFECO (Ley Federal de Protección al Consumidor):
   - Derecho a devolución: 30 días naturales desde la entrega.
   - Condiciones: producto sin uso, con etiquetas, en empaque original.
   - Productos on-demand: no aplica devolución por ser producción personalizada, salvo defecto de manufactura.
   - Proceso: enviar email a contacto@mikeshirts.mx con folio de venta y motivo.
   - Reembolso: a la forma de pago original, en un plazo de 15 días hábiles.
   - Productos defectuosos: reemplazo o reembolso completo, envío de retorno cubierto por Mike Shirts.
   - Cambios de talla: sujeto a disponibilidad, el cliente cubre envío de retorno.
2. Styled as prose.
3. SeoHead.
</action>
<verify>
- TypeScript compiles
</verify>
<done>[ ]</done>
</task>

<task id="08-04-04">
<name>Create PrivacyPage</name>
<files>
- frontend/src/pages/legal/PrivacyPage.tsx
</files>
<action>
1. Real content based on LFPDPPP (Ley Federal de Protección de Datos Personales en Posesión de Particulares):
   - Responsable: Mike Shirts (datos de contacto).
   - Datos recabados: nombre, email, teléfono, dirección, RFC (opcional), historial de compras.
   - Finalidades: procesamiento de pedidos, envíos, facturación, comunicación de estado de pedido.
   - Finalidades secundarias: envío de promociones (con opción de cancelar).
   - Transferencias: Mercado Pago (procesamiento de pagos), paqueterías (envío).
   - Medios para ejercer derechos ARCO: email contacto@mikeshirts.mx.
   - Cookies: uso de cookies analíticas para mejorar la experiencia, tracking de fuente de tráfico.
   - Cambios al aviso: publicados en esta página.
   - Fecha de última actualización.
2. Styled as prose.
3. SeoHead.
</action>
<verify>
- TypeScript compiles
</verify>
<done>[ ]</done>
</task>

<task id="08-04-05">
<name>Add SEO meta tags to all major pages</name>
<files>
- frontend/src/pages/HomePage.tsx
- frontend/src/pages/CatalogPage.tsx
- frontend/src/pages/ProductPage.tsx
- frontend/src/pages/CartPage.tsx
</files>
<action>
1. **HomePage**: SeoHead with brand description, logo as OG image.
2. **CatalogPage**: SeoHead with "Catálogo — Mike Shirts", catalog description.
3. **ProductPage**: Dynamic SeoHead per product:
   - title: product.name
   - description: first 160 chars of product.description
   - ogImage: main product image URL
   - ogUrl: `/producto/{slug}`
   - ogType: "product"
   - Inject JSON-LD: fetch from `/api/catalog/products/{slug}/schema/` and inject as `<script type="application/ld+json">`.
4. **CartPage**: SeoHead with noindex (cart pages shouldn't be indexed).
5. All checkout and admin pages should also have noindex.
</action>
<verify>
- TypeScript compiles
- View page source shows correct meta tags
</verify>
<done>[ ]</done>
</task>

<task id="08-04-06">
<name>Replace all placeholder routes and final wiring</name>
<files>
- frontend/src/App.tsx
</files>
<action>
1. Replace `/envios` placeholder with ShippingPolicyPage.
2. Replace `/devoluciones` placeholder with ReturnsPage.
3. Replace `/privacidad` placeholder with PrivacyPage.
4. Remove the Placeholder component entirely.
5. Verify all routes are connected to real pages.
6. Add a 404 catch-all route with "Página no encontrada" message.
</action>
<verify>
- TypeScript compiles with zero errors
- Vite build succeeds
- No placeholder routes remain
- All navigation links work
</verify>
<done>[ ]</done>
</task>

## Must-Haves

- [ ] SeoHead reusable component
- [ ] ShippingPolicyPage with real content
- [ ] ReturnsPage with PROFECO-based content
- [ ] PrivacyPage with LFPDPPP-based content
- [ ] Dynamic meta tags on Home, Catalog, Product, Cart
- [ ] Open Graph tags on ProductPage with product image
- [ ] JSON-LD injected on ProductPage
- [ ] noindex on checkout, cart, admin pages
- [ ] All placeholder routes replaced
- [ ] 404 catch-all route
